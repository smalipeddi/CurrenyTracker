<%= form_for(@user_currency) do |f| %>
  <% if @user_currency.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_currency.errors.count, "error") %> prohibited this user_currency from being saved:</h2>

      <ul>
      <% @user_currency.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
 
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :code %><br>
    <%= f.text_field :code %>
  </div>
  <div class="field">
    <%= f.label :country_id %><br>
    <%= f.text_field :country_id %>
  </div>
  <div class="actions">
  	<br />
    <%= f.submit :class=>"btn btn-default" %>
    <br />
    <br />
  </div>
<% end %>
